version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: container-app-capstone
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {if (r.statusCode !== 200) throw new Error(r.statusCode)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - app-network
    volumes:
      - app-logs:/app/logs

networks:
  app-network:
    driver: bridge

volumes:
  app-logs:
    driver: local
